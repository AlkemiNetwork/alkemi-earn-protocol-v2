"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const ava_1 = __importDefault(require("ava"));
const levenshtein_1 = require("./levenshtein");
ava_1.default('equal', t => {
    const a = [...'abc'];
    const b = [...'abc'];
    const ops = levenshtein_1.levenshtein(a, b, (a, b) => (a === b ? 'equal' : 'different'));
    t.deepEqual(ops, []);
});
ava_1.default('append', t => {
    const a = [...'abc'];
    const b = [...'abcd'];
    const ops = levenshtein_1.levenshtein(a, b, (a, b) => (a === b ? 'equal' : 'different'));
    t.deepEqual(ops, [
        {
            kind: 'append',
            updated: 'd',
        },
    ]);
});
ava_1.default('delete from end', t => {
    const a = [...'abcd'];
    const b = [...'abc'];
    const ops = levenshtein_1.levenshtein(a, b, (a, b) => (a === b ? 'equal' : 'different'));
    t.deepEqual(ops, [
        {
            kind: 'delete',
            original: 'd',
        },
    ]);
});
ava_1.default('delete from middle', t => {
    const a = [...'abc'];
    const b = [...'ac'];
    const ops = levenshtein_1.levenshtein(a, b, (a, b) => (a === b ? 'equal' : 'different'));
    t.deepEqual(ops, [
        {
            kind: 'delete',
            original: 'b',
        },
    ]);
});
ava_1.default('insert', t => {
    const a = [...'abc'];
    const b = [...'azbc'];
    const ops = levenshtein_1.levenshtein(a, b, (a, b) => (a === b ? 'equal' : 'different'));
    t.deepEqual(ops, [
        {
            kind: 'insert',
            updated: 'z',
        },
    ]);
});
//# sourceMappingURL=levenshtein.test.js.map